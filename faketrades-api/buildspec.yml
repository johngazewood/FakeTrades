---
version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
    build:
      commands:
        - echo Build started on date `date`
        - mvn install
    post_build:
      commands:
        - echo Build completed on `date`
artifact:
  files:
    - s3-to-s3-latest-build.zip
